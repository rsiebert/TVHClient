<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/media_route_menu_item"
        android:title="@string/media_route_menu_title"
        app:actionProviderClass="androidx.mediarouter.app.MediaRouteActionProvider"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/menu_search"
        android:icon="?attr/ic_menu_search"
        android:title="@android:string/search_go"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="collapseActionView|ifRoom" />
    <item
        android:id="@+id/menu_reconnect_to_server"
        android:icon="?attr/ic_menu_refresh"
        android:orderInCategory="10"
        android:title="@string/reconnect"
        app:showAsAction="collapseActionView" />
    <item
        android:id="@+id/menu_send_wake_on_lan_packet"
        android:icon="?attr/ic_menu_wol"
        android:orderInCategory="11"
        android:title="@string/send_wol"
        android:visible="false"
        app:showAsAction="collapseActionView" />

</menu>